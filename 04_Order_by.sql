SELECT * FROM actor order by first_name; -- Oderna de A, B, C ....

SELECT * FROM actor order by first_name desc; -- Esto es de manera descendente  C, B, A

SELECT * FROM actor where last_name = "RODRIGUEZ" order by first_name desc;
